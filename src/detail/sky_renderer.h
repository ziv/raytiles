#pragma once
#include "sky_shader.h"

namespace raytiles {
    struct rendering_config;
    class sky_renderer {
    public:
        // explicit sky_renderer(const rendering_config &opts);
        //
        // void draw(const Camera3D &camera, const Vector3 &world_offset);
        //
        // void set_zenith_color(float r, float g, float b, float a);
        //
        // void set_horizon_color(float r, float g, float b, float a);
        //
        // void set_time(float time);

    private:
        // sky_shader shader;
        // raii::model model;
    };
}
